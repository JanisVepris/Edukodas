{% if (user.hasPicture) %}
    <img class="responsive-img" src="{{ user|profile_pic }}" alt="{{ user.fullName }}">
{% else %}
    <img class="responsive-img" src="{{ asset('/bundles/edukodastemplate/image/profile-placeholder.png') }}" alt="{{ user.fullName }}">
{% endif %}
