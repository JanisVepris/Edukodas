{% if (app.user.hasPicture) %}
    <img class="responsive-img" src="{{ app.user|profile_pic }}" alt="{{ app.user.fullName }}">
{% else %}
    <img class="responsive-img" src="{{ asset('/bundles/edukodastemplate/image/profile-placeholder.png') }}" alt="{{ app.user.fullName }}">
{% endif %}
